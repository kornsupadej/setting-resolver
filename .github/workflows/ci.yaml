name: CI

on:
  push:
    branches:
      - '**'

jobs:
  run-lint:
    uses: kornsupadej/setting-resolver/.github/workflows/lint.yaml@master
  run-test:
    uses: kornsupadej/setting-resolver/.github/workflows/test.yaml@master
